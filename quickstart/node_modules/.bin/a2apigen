#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../angular2-swagger-client-generator/bin/a2apigen.js" "$@"
  ret=$?
else 
  node  "$basedir/../angular2-swagger-client-generator/bin/a2apigen.js" "$@"
  ret=$?
fi
exit $ret
